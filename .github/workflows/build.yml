name: build

on:
  push:
    branches: [ main, apiv4 ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      id-token: write

    steps:
    - uses: actions/checkout@v7

    - name: Setup .NET
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: |
          8.0.x
          9.0.x
          10.0.x

    - name: Install dependencies
      run: dotnet restore

    - name: Check vulnerable NuGet packages
      uses: elmahio/github-check-vulnerable-nuget-packages-action@v2
      with:
        projects: |
          src/Elmah.Io.Client.Extensions.Correlation/Elmah.Io.Client.Extensions.Correlation.csproj

    - name: Build
      run: dotnet build --configuration Release --no-restore

    - name: Test
      run: dotnet test --no-restore

    - name: Pack
      run: dotnet pack --configuration Release /p:Version=6.0.${{ github.run_number }}-pre

    - name: Install dotnet-validate
      run: dotnet tool install --global dotnet-validate --version 0.0.1-preview.582

    - name: Validate
      run: dotnet-validate package local src/Elmah.Io.Client.Extensions.Correlation/bin/Release/Elmah.Io.Client.Extensions.Correlation.6.0.${{ github.run_number }}-pre.nupkg

    - name: NuGet login (OIDC → temp API key)
      uses: NuGet/login@v1
      id: nuget-login
      with:
        user: ${{ secrets.NUGET_USER }}
      if: ${{ github.event_name == 'push' }}

    - name: Push
      run: dotnet nuget push src/Elmah.Io.Client.Extensions.Correlation/bin/Release/Elmah.Io.Client.Extensions.Correlation.6.0.${{ github.run_number }}-pre.nupkg --api-key ${{ steps.nuget-login.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json
      if: ${{ github.event_name == 'push' }}

    # - name: Refresh Bugster
    #   uses: elmahio/actions/refresh-bugster-repository@v1.2
    #   with:
    #     signing: ${{ secrets.BUGSTER_SIGNING_KEY }}
    #     url: ${{ secrets.FUNCTION_APP_URL }}
    #     repository: ${{ github.event.repository.name }}
    #   if: ${{ github.event_name == 'push' && github.ref_name == 'main' }}